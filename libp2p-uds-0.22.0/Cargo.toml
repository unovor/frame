# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies
#
# If you believe there's an error in this file please file an
# issue against the rust-lang/cargo repository. If you're
# editing this file be aware that the upstream Cargo.toml
# will likely look very different (and much more reasonable)

[package]
edition = "2018"
name = "libp2p-uds"
version = "0.22.0"
authors = ["Parity Technologies <admin@parity.io>"]
description = "Unix domain sockets transport for libp2p"
keywords = ["peer-to-peer", "libp2p", "networking"]
categories = ["network-programming", "asynchronous"]
license = "MIT"
repository = "https://github.com/libp2p/rust-libp2p"

[features]
default = ["async-std"]
[target."cfg(all(unix, not(target_os = \"emscripten\")))".dependencies.async-std]
version = "1.6.2"
optional = true

[target."cfg(all(unix, not(target_os = \"emscripten\")))".dependencies.futures]
version = "0.3.1"

[target."cfg(all(unix, not(target_os = \"emscripten\")))".dependencies.libp2p-core]
version = "0.22.0"

[target."cfg(all(unix, not(target_os = \"emscripten\")))".dependencies.log]
version = "0.4.1"

[target."cfg(all(unix, not(target_os = \"emscripten\")))".dependencies.tokio]
version = "0.2"
features = ["uds"]
optional = true
default-features = false
[target."cfg(all(unix, not(target_os = \"emscripten\")))".dev-dependencies.tempfile]
version = "3.0"
